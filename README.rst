Assorted utility functions
==========================
 
These are some assorted utility functions.

.. code-block:: bash

$ sudo python setup.py install

or

.. code-block:: bash

$ sudo python setup.py develop

should be sufficient to install this package.
